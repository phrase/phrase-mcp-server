name: License Check
on: [push]

jobs:
  license-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: License check
        uses: phrase/actions/lawa-ci@0dd6e55527752d2ade1981db61c3bb436377c798 # main
        with:
          decisions-file: ./license-decisions.yml
          package-managers: npm
          github-token: ${{ secrets.GH_LAWA_ACCESS_TOKEN }}
